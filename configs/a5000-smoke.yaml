# Development-only smoke run. Do not report these one-seed/subsampled results in the paper.
experiment:
  name: fedicl_mqa_a5000_smoke
  output_dir: outputs/a5000-smoke
  data_seed: 20260901
  training_seeds: [42]
  deterministic: true

data:
  dataset: medqa
  max_train_samples: 1000
  max_validation_samples: 200
  max_test_samples: 200
